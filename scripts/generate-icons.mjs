import { writeFileSync, mkdirSync } from "node:fs";
import { join, dirname } from "node:path";
import { fileURLToPath } from "node:url";

const __dirname = dirname(fileURLToPath(import.meta.url));
const iconsDir = join(__dirname, "..", "public", "icons");
mkdirSync(iconsDir, { recursive: true });

// 1x1 purple pixel PNG — replace with branded assets before production
const PLACEHOLDER_PNG = Buffer.from(
  "iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNk+M9QDwADhgGAWjR9awAAAABJRU5ErkJggg==",
  "base64",
);

writeFileSync(join(iconsDir, "icon-192.png"), PLACEHOLDER_PNG);
writeFileSync(join(iconsDir, "icon-512.png"), PLACEHOLDER_PNG);

console.log("Placeholder icons written to public/icons/");
